temporal:
  url: localhost:7233
  namespace: default
  task_queue: easyeasm_demo

neo4j:
  password: supertestovaciheslo
  bolt: bolt://localhost:7687
  user: neo4j

redis:
  host: localhost
  port: 6379

isim:
  url: "http://localhost:8000"

nmap_basic:
  targets:
    - localhost
  arguments: "-sV -A"

nmap_topology:
  targets:
    - localhost
  arguments: "-sn -n --traceroute"

easm_scanner:
  domains:
    - vulnweb.com
  mode: complete  # fast or complete
  httpx_path: "path/to/httpx"
  threads: 100  # required only for complete mode
  wordlist_path: "path/to/wordlist.txt"  # required only for complete mode